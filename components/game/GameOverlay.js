
"use client"

export default function GameOverlay(){

  return (

    <div

      style={{

        position:"fixed",

        inset:0,

        pointerEvents:"none",

        background:
        "radial-gradient(circle,transparent,rgba(0,0,0,0.4))"
      }}
    />
  )
}
